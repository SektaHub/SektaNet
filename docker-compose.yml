version: '3.8'services:  backend:    image: 'sektahub-backend'    ports:      - "8080:8080"    networks:       - sektanetwork  frontend:    image: 'sekta-frontend'    ports:      - "3000:3000"    networks:       - sektanetwork  database:    container_name: "baza"    image: 'postgres'    ports:      - "5433:5432"    volumes:      - ./docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d    networks:       - sektanetwork    environment:       POSTGRES_PASSWORD: "sektaadmin"    # Custom command or entrypoint if needed    #command: /docker-entrypoint-initdb.d/install-pgvector.shnetworks:  sektanetwork: